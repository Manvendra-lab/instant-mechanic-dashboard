# Instant Mechanic - Live Operations Dashboard

## Project Overview
A production-ready Live Vehicle Service Operations Dashboard built for the Instant Mechanic operations team. This system monitors bookings in real-time, allowing operators to search, filter by service category, and track status changes without page reloads.

## Tech Stack
* **Frontend:** Next.js, React, TypeScript, Tailwind CSS, shadcn/ui
* **Backend:** Python, FastAPI, WebSockets
* **Database:** MongoDB Atlas
* **Deployment:** Vercel (Frontend), Render (Backend)

Note on Deployment Pivot: The original assignment suggested AWS EC2 for backend hosting. Due to AWS account/billing restrictions, I adapted the architecture and deployed the FastAPI backend to Render. This successfully maintains a live, publicly accessible API with full WebSocket support for the real-time dashboard requirements.

## Architecture
Frontend (Next.js/React) 
  ↓ REST for initial data load / WebSockets for live updates
API (FastAPI) 
  ↓ Asynchronous queries (Motor)
Backend 
  ↓
Database (MongoDB Atlas cluster)

## Bonus Features Implemented
* **WebSockets:** Integrated real-time status updates via WebSocket connection manager so the dashboard reflects changes without a page reload.
* **Swagger/OpenAPI:** Auto-generated interactive API documentation available at the `/docs` endpoint via FastAPI.
* **API Rate Limiting:** Implemented `slowapi` to throttle incoming requests and protect backend endpoints.
* **Advanced Filters:** Built a chained filtering system utilizing a live search bar and dynamic sidebar category tabs.
* **Responsive Mobile Dashboard:** Utilized Tailwind CSS to ensure the layout seamlessly adapts to mobile screens.

## Local Setup
1. Clone the repository: `git clone <repo-link>`
2. **Backend:** 
   * `cd backend`
   * `python -m venv venv`
   * `source venv/bin/activate`
   * `pip install -r requirements.txt`
   * Add MongoDB connection string to `main.py`
   * Run: `uvicorn main:app --reload`
3. **Frontend:**
   * `cd frontend`
   * `npm install`
   * Run: `npm run dev`

## API Documentation
Interactive Swagger documentation is automatically generated by FastAPI. 
Access it live at: `http://<YOUR_EC2_IP>:8000/docs`
* `GET /api/bookings` - Fetches paginated bookings.
* `PUT /api/bookings/{id}/status` - Updates status and broadcasts WebSocket event.

## Live Links
* **Frontend (Vercel):** https://your-vercel-link.vercel.app
* **Backend API (AWS):** http://<YOUR_EC2_IP>:8000
* **API Docs:** http://<YOUR_EC2_IP>:8000/docs

## AI Usage
* **ChatGPT/Claude:** Used to generate the realistic `seed.py` data generation script, generate the dynamic Tailwind color mapping for status badges, and assist with configuring the Next.js flexbox layout for the sidebar.
* **Manual Implementation:** I manually architected the WebSocket Connection Manager in Python, designed the MongoDB schemas, integrated the frontend hooks to prevent memory leaks, and managed the AWS EC2 deployment process.

